#ifndef _AUTOFOV_CORE_H_
#define _AUTOFOV_CORE_H_

#ifdef __cplusplus
extern "C" {
#endif

/* for unused function parameters */
#define	UNUSED(x)	(void)(x)

/* comparison */
#define MAX(a,b) ((a) >= (b) ? (a) : (b))
#define MIN(a,b) ((a) <= (b) ? (a) : (b))

#ifdef	__cplusplus
}
#endif

#endif /* _AUTOFOV_CORE_H_ */
